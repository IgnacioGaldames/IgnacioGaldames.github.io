---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign pages = site.pages | where_exp: "item", "item.title != nil" %}
  {% for page in pages %}
    {% if page.url contains '.xml' or page.url contains 'assets' or page.url contains '404' or page.url contains 'README' %}{% else %}
      <url>
        <loc>{{ site.url }}{{ page.url | replace: 'index.html', '' }}</loc>
        {% if page.last_modified_at %}
        <lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>
        {% else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
        {% endif %}
        <changefreq>weekly</changefreq>
        {% if page.url == '/' %}
        <priority>1.0</priority>
        {% elsif page.url contains 'wiki/' %}
        <priority>0.6</priority>
        {% else %}
        <priority>0.8</priority>
        {% endif %}
      </url>
    {% endif %}
  {% endfor %}
  
  {% for post in site.posts %}
      <url>
        <loc>{{ site.url }}{{ post.url | replace: 'index.html', '' }}</loc>
        {% if post.last_modified_at %}
        <lastmod>{{ post.last_modified_at | date_to_xmlschema }}</lastmod>
        {% else %}
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
        {% endif %}
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
      </url>
  {% endfor %}

  {% for page in site.libros %}
      <url>
        <loc>{{ site.url }}{{ page.url }}</loc>
        {% if page.last_modified_at %}
        <lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>
        {% else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
        {% endif %}
        <changefreq>monthly</changefreq>
        <priority>0.9</priority>
      </url>
  {% endfor %}

  {% for page in site.cuentos %}
      <url>
        <loc>{{ site.url }}{{ page.url }}</loc>
        {% if page.last_modified_at %}
        <lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>
        {% else %}
        <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
        {% endif %}
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
      </url>
  {% endfor %}
</urlset>